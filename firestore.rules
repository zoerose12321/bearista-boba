rules_version = '2';

// DEVELOPMENT ONLY — prototype online café sessions (v0.1.61).
// TODO: Replace with authenticated, scoped rules before production release.
service cloud.firestore {
  match /databases/{database}/documents {
    match /onlineCafeSessions/{sessionId} {
      allow read, write: if true;
    }
  }
}
